spring:
  main:
    lazy-initialization: true
    banner-mode: off
  datasource:
    name: cloudmold-listing-test
    url: jdbc:h2:mem:cloudmold_listing;MODE=MYSQL;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:
    druid:
      async-init: true
      initial-size: 1
  sql:
    init:
      schema-locations: classpath:/sql/create_tables.sql

mybatis:
  lazy-initialization: true

yudao:
  info:
    base-package: cn.iocoder.yudao.module.cloudmold.listing
